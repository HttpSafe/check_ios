{
	"name": "check_ios",
	"version": "1.0.2",
	"description": "Check if the device is a IOS device and to check if the device is iPad or not",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "tsc index.ts"
	},
	"author": "Sterin Saji",
	"license": "ISC",
	"repository": {
		"type": "git",
		"url": "git@github.com:HttpSafe/check_ios.git"
	},
	"devDependencies": {
		"@types/node": "^20.4.0",
		"typescript": "^5.1.6"
	}
}
